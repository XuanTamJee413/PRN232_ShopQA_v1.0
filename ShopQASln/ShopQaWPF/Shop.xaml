<Window x:Class="ShopQaWPF.Shop"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ShopQaWPF"
        Title="Shop" Height="720" Width="1080">
    <Grid Margin="10">
        <StackPanel Orientation="Vertical">
            <!-- Header dùng chung -->
            <local:HeaderControl HorizontalAlignment="Stretch"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <ComboBox x:Name="cbCategory" Width="150" Margin="0,0,10,0" />
                <ComboBox x:Name="cbBrand" Width="150" Margin="0,0,10,0" />
                <TextBox x:Name="txtSearch" Width="200" Margin="0,0,10,0"/>
                <Button Content="Tìm" Click="Search_Click"/>
            </StackPanel>

            <ListView x:Name="lvProducts" Height="600">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Hình ảnh" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Image Source="{Binding ImageUrl}" Width="80" Height="80" Stretch="UniformToFill"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Tên" DisplayMemberBinding="{Binding Name}" Width="200"/>
                        <GridViewColumn Header="Mô tả" DisplayMemberBinding="{Binding Description}" Width="300"/>
                        
                        <GridViewColumn Header="Chi tiết" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Xem" Click="Detail_Click" Tag="{Binding}" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Yêu thích" Width="60">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="♥" Foreground="#E53935" FontSize="18" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="AddToWishlist_Click" Tag="{Binding}" ToolTip="Thêm vào yêu thích"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>
    </Grid>
</Window>
