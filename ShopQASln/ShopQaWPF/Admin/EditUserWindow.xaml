<Window x:Class="ShopQaWPF.Admin.EditUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit User" Height="400" Width="400" WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Username -->
        <StackPanel Grid.Row="0" Margin="0,10">
            <TextBlock Text="Username" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="UsernameBox" Height="30" />
        </StackPanel>

        <!-- Email -->
        <StackPanel Grid.Row="1" Margin="0,10">
            <TextBlock Text="Email" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="EmailBox" Height="30" />
        </StackPanel>

     

        <!-- Role -->
        <StackPanel Grid.Row="3" Margin="0,10">
            <TextBlock Text="Role" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <ComboBox x:Name="RoleBox" Height="30">
                <ComboBoxItem Content="Customer" />
                <ComboBoxItem Content="Staff" />
                <ComboBoxItem Content="Admin" />
            </ComboBox>
        </StackPanel>

        <!-- Save Button -->
        <Button Content="Update"
                Grid.Row="4"
                Height="40"
                Margin="0,20,0,0"
                Background="#2196F3"
                Foreground="White"
                FontWeight="Bold"
                Click="SaveButton_Click"/>
    </Grid>
</Window>
